git_src --branch v256.13 https://github.com/systemd/systemd.git
tmp_deb=$(mktemp -d)
git clone --depth 1 --recurse-submodules -b "debian/256.7-3" https://salsa.debian.org/systemd-team/systemd.git "$tmp_deb"
cp -r "${tmp_deb}/debian" "${dir}/src/"
rm -rf "${tmp_deb}"
import_upstream_patches
apply_patches

version="256.13-0"
message="Update to 256.13-0"
version_suffix=gl0
